# TODO - 코드 복잡성 개선 및 리팩토링 계획

> **현재 상태**: 다중 작업으로 인해 코드베이스 복잡성이 증가했습니다. 이 문서는 유지보수성을 향상시키고 복잡성을 줄이기 위한 종합적인 개선 계획을 제시합니다.

## 🚨 **긴급 해결 필요 (Critical Issues)**

### 1. 아키텍처 복잡성 해결

**문제**: `Main` 클래스가 너무 많은 책임을 가지고 있음 (오케스트레이션, 서비스 초기화, 워크플로우 관리)

**해결책**:

- [ ] `WorkflowOrchestrator` 클래스 추출하여 문서 처리 흐름 분리
- [ ] `ServiceFactory` 생성하여 의존성 주입 구현
- [ ] 초기화 로직과 비즈니스 로직 분리

### 2. 서비스 레이어 과도한 결합

**문제**: 서비스들이 복잡한 상호 의존성으로 강하게 결합됨

**해결책**:

- [ ] 서비스 인터페이스/프로토콜 구현 (`ITaskCardService`, `IReceptionService`)
- [ ] 의존성 주입 컨테이너 사용
- [ ] 서비스 레이어 추상화 생성

### 3. `OfficialCollector` 클래스 비대화 (700+ 라인)

**문제**: RPA 로직, UI 처리, 비즈니스 로직이 하나의 클래스에 혼재

**해결책**:

- [ ] RPA 자동화와 비즈니스 로직 분리
- [ ] UI 상호작용을 별도 서비스로 추출
- [ ] 단일 책임 원칙 적용하여 클래스 분할

## 🔴 **고우선순위 개선사항**

### 4. 에러 처리 일관성 부족

**문제**: 코드베이스 전반에 걸쳐 혼재된 에러 처리 패턴

**해결책**:

- [ ] 커스텀 예외 클래스 계층 구조 생성
- [ ] 중앙화된 에러 리포팅 구현
- [ ] 에러 복구 메커니즘 추가

### 5. 순환 의존성 문제

**문제**: 여러 모듈 간 순환 의존성 존재

**해결책**:

- [ ] 의존성 그래프 분석 및 재구성
- [ ] 인터페이스를 통한 의존성 역전
- [ ] 모듈 경계 명확화

### 6. 설정 관리 분산

**문제**: 설정이 여러 파일에 분산되어 관리 어려움

**해결책**:

- [ ] 모든 설정을 `config.py`에 중앙화
- [ ] 설정 검증 로직 추가
- [ ] 다중 환경 설정 지원

## 🟡 **중우선순위 개선사항**

### 7. UI/비즈니스 로직 혼재

**문제**: 다이얼로그 처리와 비즈니스 로직이 혼재됨

**해결책**:

- [ ] UI 컴포넌트를 별도 레이어로 분리
- [ ] 사용자 상호작용 패턴 추상화
- [ ] 커맨드 패턴으로 사용자 액션 구현

### 8. 데이터 접근 불일치

**문제**: 여러 데이터 소스와 형식의 비일관적 처리

**해결책**:

- [ ] 통합 데이터 접근 레이어 생성
- [ ] 데이터 작업을 위한 Repository 패턴 구현
- [ ] 데이터 검증 및 변환 유틸리티 추가

### 9. 타입 힌팅 & 문서화 부족

**해결책**:

- [ ] 모든 함수/메서드에 완전한 타입 힌팅 적용
- [ ] docstring 표준화 및 완성
- [ ] API 문서 자동 생성 도구 도입

## 📋 **구체적인 리팩토링 단계**

### Phase 1: 핵심 아키텍처 재구성 (1-2주)

- [ ] `Main` 클래스에서 `WorkflowOrchestrator` 추출
- [ ] `ServiceContainer` 구현하여 의존성 주입
- [ ] `OfficialCollector`를 RPA 작업과 비즈니스 로직으로 분리
- [ ] 서비스 인터페이스 정의 (`ITaskCardService`, `IReceptionService`, `IDialogService`)

### Phase 2: 서비스 레이어 개선 (1-2주)

- [ ] `DocumentProcessor` 추상 기본 클래스 생성
- [ ] `ApprovalDocumentProcessor`와 `ReceptionDocumentProcessor` 구현
- [ ] 다이얼로그 처리를 `UserInteractionService`로 추출
- [ ] `VectorStoreManager`로 Chroma 작업 관리

### Phase 3: 에러 처리 & 검증 (1주)

- [ ] 커스텀 예외 계층 구조 (`RpaException`, `ProcessingException` 등)
- [ ] 재시도 메커니즘이 있는 `ErrorHandler` 구현
- [ ] 입력 검증 프레임워크 추가
- [ ] 에러 리포팅 및 로깅 전략 수립

### Phase 4: 데이터 & 설정 관리 (1주)

- [ ] 구현체가 있는 `DataRepository` 인터페이스 생성
- [ ] 환경 지원이 있는 `ConfigurationManager` 구현
- [ ] 데이터 변환 유틸리티 추가
- [ ] 백업 및 복구 메커니즘 생성

## 🏗️ **제안하는 새로운 구조**

```
src/
├── core/
│   ├── interfaces/          # 서비스 인터페이스 및 프로토콜
│   ├── exceptions/          # 커스텀 예외 클래스
│   └── container.py         # 의존성 주입 컨테이너
├── services/
│   ├── orchestration/       # 워크플로우 오케스트레이션
│   ├── document/           # 문서 처리 서비스
│   ├── rpa/               # RPA 자동화 서비스
│   ├── data/              # 데이터 접근 서비스
│   └── user/              # 사용자 상호작용 서비스
├── infrastructure/
│   ├── vector_stores/      # 벡터 데이터베이스 구현
│   ├── config/            # 설정 관리
│   └── logging/           # 로깅 인프라
├── domain/
│   ├── models/            # 도메인 모델 및 엔티티
│   └── validators/        # 도메인 검증 로직
└── ui/
    ├── cli/               # 명령줄 인터페이스
    └── dialogs/           # 다이얼로그 관리
```

## 🎯 **우선순위 및 타임라인**

### 🚨 긴급 (1-2주 내)

1. **Main 클래스 분해**: 단일 책임 원칙 적용
2. **OfficialCollector 리팩토링**: 700+ 라인 클래스 분할
3. **에러 처리 일관성**: 전체 코드베이스 표준화

### 🔴 고우선순위 (2-4주 내)

1. **서비스 레이어 해결**: 의존성 주입 및 인터페이스
2. **테스트 인프라**: 기존 기능에 대한 80% 커버리지
3. **설정 중앙화**: 환경별 설정 관리

### 🟡 중우선순위 (1-2달 내)

1. **성능 최적화**: 벡터 검색 및 RPA 작업
2. **UI/UX 개선**: 사용자 상호작용 향상
3. **문서화**: API 문서 및 아키텍처 가이드

## 📊 **성공 지표**

### 리팩토링 전 현재 상태

- Main.py: ~200라인 (다중 책임)
- OfficialCollector.py: ~700+라인 (UI/RPA/비즈니스 로직 혼재)
- 서비스들 간 강한 결합 및 순환 의존성
- 일관되지 않은 에러 처리 패턴

### 리팩토링 후 목표

- 단일 클래스 300라인 이하
- UI, 비즈니스 로직, 인프라 간 명확한 분리
- 의존성 주입으로 느슨한 결합 서비스
- 일관된 에러 처리 및 로깅
- 80% 이상 테스트 커버리지
- 1초 이하 시작 시간
- 유지보수 가능하고 확장 가능한 아키텍처

## 🛠️ **구현 가이드라인**

### 코드 품질 표준

- 최대 순환 복잡도: 10
- 최대 함수 길이: 50라인
- 최대 클래스 길이: 300라인
- 최소 테스트 커버리지: 80%

### 설계 원칙

- 단일 책임 원칙 (SRP)
- 의존성 역전 원칙 (DIP)
- 인터페이스 분리 원칙 (ISP)
- DRY (Don't Repeat Yourself)

## 🚀 **즉시 시작할 수 있는 작업**

1. **리팩토링용 feature 브랜치 생성**
2. **Main 클래스에서 WorkflowOrchestrator 추출 시작**
3. **서비스 인터페이스 및 의존성 주입 구현**
4. **리팩토링 전 기존 기능에 대한 종합 테스트 추가**
5. **위험 최소화를 위해 한 번에 하나의 서비스씩 리팩토링**

---

**마지막 업데이트**: 2025-07-30  
**우선순위**: 높음 - 코드 유지보수성 및 확장성
