<!-- Trace:
spec_id: SPEC-governance-doc-structure-1
task_id: TASK-001
-->
# Code Review Notes (Redirect)

Find actionable review items mapped as follows:
- Config + Supabase hardening issues -> `.tasks/backlog.yaml` (`TASK-004`, `TASK-006`) and `.spec/supabase-migration/spec.yaml`.
- Performance/logging follow-ups -> `.tasks/backlog.yaml` (`TASK-003`) and `.spec/performance-observability/spec.yaml`.
- DocumentProcessor UX fixes -> `.tasks/backlog.yaml` (`TASK-005`) referencing `.spec/core-automation/spec.yaml`.

Refer to those structured files for the latest status; this document now serves as a breadcrumb to the new SDD/TDD sources of truth.
